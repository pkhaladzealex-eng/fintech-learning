# Stripe Payment Setup (Sandbox Mode)

This folder contains a screenshot of my Stripe Dashboard in Test Mode. 

### Key Observations from the Dashboard:
* **Environment:** I am currently in the **Sandbox** environment, which allows for testing payments without using real money.
* **Currency:** The dashboard is configured for the Czech Republic, showing balances in **CZK (Kč)**.
* **API Readiness:** The "API keys" section is visible on the right, providing the `pk_test` (Publishable) and `sk_test` (Secret) keys needed for Python integration.
* **Overview:** The "Today" and "Total Volume" sections are ready to track incoming test transactions once I start sending API requests.

### Next Steps:
My goal is to use this setup to practice API testing and automate payment flows using Python and the `stripe` library.